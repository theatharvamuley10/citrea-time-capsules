import React from "react";
import "./ScrollPrompt.css";

const ScrollPrompt = () => (
  <div className="scroll-prompt">
    <span className="prompt-text">Create a time capsule now</span>
    <span className="arrow">&#8595;</span>
  </div>
);

export default ScrollPrompt;
